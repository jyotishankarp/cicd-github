{
    "name": "my-nodejs-lambda",
    "version": "1.0.0",
    "description": "Node.js Lambda function",
    "main": "index.js",
    "scripts": {
        "test": "jest",
        "build": "npm ci --production && npm run bundle",
        "bundle": "zip -r function.zip . -x '*.git*' '*.github*' '*.zip' 'node_modules/aws-sdk/*'"
    },
    "dependencies": {
        "aws-sdk": "^2.1692.0",
        "express": "^4.21.2",
        "serverless-http": "^3.2.0"
    },
    "devDependencies": {
        "jest": "^29.5.0",
        "serverless-offline": "^14.4.0"
    }
}
